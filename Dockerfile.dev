# Development Dockerfile — hot reload with volumes
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json* ./
RUN npm install

# Copy source (overridden by volume mount in docker-compose)
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
